import { FiltrerZonePipe } from './filtrer-zone.pipe';

describe('FiltrerZonePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerZonePipe();
    expect(pipe).toBeTruthy();
  });
});
